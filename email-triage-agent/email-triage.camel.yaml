- route:
    id: read-emails
    from:
      uri: imaps
      parameters:
        delete: "false"
        host: //imap.gmail.com
        unseen: "true"
      steps:
        - log:
            message: "Subject: ${header.Subject}"
        - log:
            message: "From: ${header.From}"
        - log:
            message: "Body: ${body}"
